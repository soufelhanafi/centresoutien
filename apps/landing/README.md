# apps/landing — placeholder

The public marketing site (**centresoutien.com**) will be imported here as its
own focused migration PR — preserving its git history (`git subtree` /
`git-filter-repo`), reconfiguring Vercel to a monorepo **Root Directory** of
`apps/landing`, and reconciling its Tailwind tokens with the shared
`packages/config` design tokens (SOU-22).

Until then this directory is an intentional placeholder so the workspace graph
reserves the slot. **Do not** copy the landing source in ad-hoc — see the
decision recorded for this project.
