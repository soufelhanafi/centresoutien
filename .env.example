# Centre Soutien — environment variables (example)
#
# Copy this file to `.env` and fill in real values. `.env` is gitignored and must
# NEVER be committed. These variables are read ONLY in the Electron main process.
